# 安全与数据边界

此版本用于本地开发与演示，不建议直接暴露到互联网。

## 配置

- 首次启动修改管理员密码、数据库密码和签名密钥。不要使用示例默认值公开部署。
- 本地管理端口仅绑定 127.0.0.1。Traefik 是可选开发入口，其配置不能直接视为生产安全配置。
- 不要公开 `.env`、`.env.ai`、完整的 `docker compose config` 输出、数据库备份、登录存储文件、浏览器测试 trace 或含用户数据的日志。
- AI 请求需要明确外发确认；业务资料可能发送给所配置的模型服务。不要上传没有处理权限的企业文件。

## 已有约束

后端对角色与工厂对象范围分别授权；模型工具不能选择其他工厂或执行任意 SQL。知识更新带版本检查；回复返回前再次核对权限及引用版本。文件导入检查路径范围、大小、映射和内容散列。

数据库测试有 `_test` 库名拒绝保护；文件测试和浏览器测试仍会修改其测试环境的数据，隔离库不是备份。

## 尚未覆盖

真实工业设备证书与厂商兼容性、分布式采集租约、全局限流、生产监控告警、定时备份与恢复目标尚未完整验收。模型引用检查不能证明自然语言结论必然正确；设备控制和生产决策不属于本系统职责。

发现安全问题时请勿把密钥、用户数据或可利用细节直接贴入公开 Issue。先通过仓库维护者提供的私密渠道联系；尚无可用私密渠道时，只公开不含利用细节的联系请求。
