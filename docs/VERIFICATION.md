# 验证记录

## 发布前源码回归（2026-09-10）

私有源码基于 be905e7 的业务代码与测试：WSL2、Python 3.14.5、PostgreSQL 18.6，专用测试数据库。

- 后端 245 项通过、2 项显式付费测试跳过，49.01 秒，语句覆盖率 92%。
- 4 项测试入口检查通过；静态、类型与生成客户端检查通过。
- 新增 4 项采集恢复测试：数据库未就绪后恢复、失败任务重建而健康任务不重启、数据库故障时取消清理、写入任务失败传递与连接释放。
- 首轮执行遇到数据库服务重启，出现 AdminShutdown，结果不作为通过证据；环境恢复后完整重跑。

这是发布前源码记录，不替代公开快照自身的 CI。最新结果请查看本仓库 Actions。

## 历史性能实验

| 场景 | 样本与统计 | 结果 |
| --- | --- | --- |
| 文件导入 | 单数值测点、10 万行合成 CSV、每版 3 次、中位数、不启用 tracemalloc | 18.783 → 6.927 秒 |
| 最新值查询 | 32 测点、20 万条合成样本、每版 20 次热缓存查询、中位数 | 37.756 → 1.323 毫秒 |

查询新旧结果一致。导入比较保留字段校验、唯一键冲突处理、返回 ID 计数和整批事务。导入详情见 IMPORT_PERFORMANCE_20260908.md。这些是归档实验，不是此快照重新压测或生产 SLA；不包含 HTTP 上传、排队和并发用户延迟。

## 如何复现

- 后端：完成数据库与邮件服务启动后，在根目录运行 `bash scripts/test-local.sh`。
- 入口安全：`uv run python -m unittest discover -s scripts/tests -v`，不访问数据库。
- 静态：`uv run prek run --all-files`。
- 页面：在临时 Compose 环境运行 Playwright 工作流，不对日常业务库运行整套浏览器测试。
- 导入性能：阅读 `backend/scripts/benchmark-import-jobs.sh` 的专用测试库与全新输出目录要求。
- 查询性能：使用 `backend/scripts/benchmark-latest.sh`，先检查脚本的测试库保护及参数。

性能实验不要和构建、备份或其他压测并行；记录完整样本，不只挑最佳结果。

## AI 评测口径

仓库保留合成评测语料及运行脚本。既有 26 场景反复运行属于功能回归，不是独立问答准确率。HTTP 成功、JSON 合法、检索命中及引用存在都不能代替人工语义评判。

真实模型测试默认关闭，显式开启会产生 API 用量并外发合成资料。生产模型、知识库变化后，需要新的预留题与人工复核，不能直接复用旧结论。
