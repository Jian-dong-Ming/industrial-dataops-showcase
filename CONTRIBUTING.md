# 开发约定

修改前先阅读 README、development.md 和对应模块说明。

1. 从 main 创建短期功能分支，一次 PR 聚焦一类问题。
2. 接口、权限、数据模型或状态机变更需要对应回归测试；模型生成的代码同样需要人工理解与验证。
3. 数据库结构通过 Alembic 迁移，不直接手改生产表。破坏性变更必须写明备份与回滚边界。
4. 运行静态检查与隔离测试，确认没有提交密钥、测试登录状态、业务文件和生成缓存。
5. 性能结论记录数据规模、环境、重复次数和统计口径；不能用最好的一次结果代表整体性能。
6. PR 描述说明问题、方案、测试结果和剩余限制。不提交与项目无关的个人材料。

仓库采用 MIT 许可，贡献代码需具有相应授权。请保留上游许可和来源说明。
